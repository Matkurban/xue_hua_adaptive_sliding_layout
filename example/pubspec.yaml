name: xue_hua_adaptive_sliding_layout_example
description: Example host for xue_hua_adaptive_sliding_layout.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  signals_flutter: ^7.1.0
  xue_hua_adaptive_sliding_layout:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
